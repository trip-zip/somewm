output
  roots flow 1 floating 0 derived 0
  Clay__RootContainer w=fixed(1280) h=fixed(720) output row
    OUTPUT WL-1 w=fixed(1280) h=fixed(720) output column image
      LAYER_BAR test-layer-bar w=grow h=fixed(40) protocol row
        layer.surface test-layer-bar w=grow h=fixed(40) protocol custom
      WIBAR screen 1 converted clip w=grow h=fixed(28) theme row
        text "bar" font=1
      WORKAREA - w=grow h=grow row
