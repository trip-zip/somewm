output
  roots flow 1 floating 1 derived 0
  OUTPUT WL-1 w=fixed(1280) h=fixed(720) output column
    BACKGROUND WL-1 w=grow h=grow output attach PARENT band -10 image
    WIBAR screen 1 converted clip w=grow h=fixed(28) theme row
      text "wifi" font=1
    WORKAREA - w=grow h=grow row
  TOOLTIP screen 1 converted clip w=fit h=fit row pad 4,4,4,4 attach ELEMENT band 80
    text "Connected to Home Wi-Fi" font=1
