output
  roots flow 1 floating 2 derived 0
  OUTPUT WL-1 w=fixed(1280) h=fixed(720) output column
    BACKGROUND WL-1 w=grow h=grow output row attach PARENT band -10
    WIBAR screen 1 converted w=grow h=fixed(32) theme row
        drawable clip w=grow h=grow
    WORKAREA - w=grow h=grow row
      CLIENT band_tile w=grow h=grow theme column pad 1,1,1,1
        SURFACE band_tile w=grow h=grow custom
  CLIENT band_blue w=fixed(202) h=fixed(186) user column pad 1,1,1,1 attach OUTPUT band 20
    TITLEBAR band_blue converted w=grow h=fixed(24) theme row
        drawable clip w=grow h=grow
    SURFACE band_blue w=grow h=grow custom
  CLIENT band_red w=fixed(202) h=fixed(186) user column pad 1,1,1,1 attach OUTPUT band 20
    TITLEBAR band_red converted w=grow h=fixed(24) theme row
        drawable clip w=grow h=grow
    SURFACE band_red w=grow h=grow custom
